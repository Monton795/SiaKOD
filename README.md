# Task 1: Поиск повторяющегося элемента

Три способа решения:
1. Множество — O(n) время, O(n) память.
2. Сортировка — O(n log n) время, O(n) память.
3. Алгоритм Флойда — O(n) время, O(1) память.

## Запуск
```bash
python task1.py